// Prisma Schema — Proyecto Semana 05
// Adapta los modelos al dominio asignado por el instructor

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============================================================
// TODO: Define tus modelos según tu dominio asignado
//
// Lineamientos:
//   - Recurso principal con mínimo 6 campos tipados
//   - Al menos un campo @unique (para demostrar P2002)
//   - Recurso secundario con relación 1:N
//   - Timestamps: createdAt y updatedAt
//
// La PK SIEMPRE es UUID: id String @id @default(uuid()) @db.Uuid
// Las FK que apuntan a ella también: authorId String @db.Uuid
//
// Ejemplos:
//
// Biblioteca:
//   model Author { id String @id @default(uuid()) @db.Uuid; name String; ... }
//   model Book   { id String @id @default(uuid()) @db.Uuid; isbn String @unique; authorId String @db.Uuid; ... }
//
// Farmacia:
//   model Category   { id String @id ...; name String @unique; ... }
//   model Medication { id String @id ...; sku String @unique; categoryId String? @db.Uuid; ... }
//
// Añade aquí tus modelos:
// ============================================================
